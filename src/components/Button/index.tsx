// Styles
import styles from "./styles.module.scss";

const Button = () => {
  //const { children, className, ...rest } = props;

  return <button className={styles.__button}>{/*children*/}</button>;
};

export default Button;
